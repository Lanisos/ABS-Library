using UnityEngine;

namespace MultipleEnums {
    public enum types_of_agents {reflex_simple, model_based};
    public enum agent_shape {circle, cube, person};
    public enum time_amount {millisecond, second, minute, hour, day, month, year};
    public enum types_of_items {decorative, static_item, interactable};
}